import SwiftUI

extension Image {
    struct Icons {
        static let icArrowLeft = Image("ic-arrow-left")
        static let icBackward = Image("ic-backward")
        static let icForward = Image("ic-forward")
        static let icMore = Image("ic-more")
        static let icMusicNote = Image("ic-music-note")
        static let icPlaylist = Image("ic-playlist")
    }
}
